Video 1: Ha explicado que tienes que cargar las funciones después de cargar el html porque si no puede que no funcione.
Video 2: declarando funciones y variables, todo muy básico.
Video 3 : declarando js externo
Video 5: 
const no se puede cambiar, es como final, es local por lo que si la llamas fuera no va
let también es local, pero se puede cambiar el valor
var no es local, es local a la función entera. También se puede cambiar el valor.
si está vacía, es global para todas las funciones.
Ha usado el eventlisteney keyup y blur para checkear los valores de un formulario
Video 6:
confirm es un metodo que hace un alert con opcion de si o no, pero para que funcione tienes que utilizar PREVENTDEFAULT
puedes usar patrones (patron.test(valor.value)==true)
puedes crear un array (var x=[]) y le agregas cosas con .put()
puedes hacer un return de una funcion y luego asignar esa funcion a una variable y tendria el valor de la variable que has returneado.
Video 7
si escribes en un string (\"entre comillas\") el caracter que va despues de la \ no se procesa.
en un sout puedes poner ${a} y es como si estubiese fuera de las ""
Video 8: 
Puedes crear un objeto abriendo una llave y declarando variables dentro, luego lo llamas en objeto["cariableinterior"]
quitas valores al array con shift para el primero y pop para el ultimo (array.shift())
Date
